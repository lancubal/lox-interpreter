// Benchmark de rendimiento: Fibonacci recursivo
fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}

var start = clock();
print fib(30);
print "Tiempo transcurrido (seg):";
print clock() - start;
